#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints "Hello, Betty!" to stdout.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    printf("Hello, Betty!\n");
    return (0);
}
